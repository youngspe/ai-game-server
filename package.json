{
  "name": "ai-game-server",
  "version": "0.1.0",
  "description": "",
  "main": "bin/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "installClient": "cd ai-game-client && npm install",
    "install": "npm run installClient",
    "buildClient": "cd ai-game-client && npm run build:dev",
    "build": "npm run buildClient && tsc",
    "start": "npm run build && node ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/uuid": "^9.0.1",
    "@types/ws": "^8.5.4",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-jwt": "^8.4.1",
    "jsonwebtoken": "^9.0.0",
    "openai": "^3.2.1",
    "uuid": "^9.0.0",
    "ws": "^8.13.0"
  }
}
